#include <stdio.h>
#include <stdlib.h>
int countUnsignedNumber;
     
     int arryNumber;
     printf("Vui lòng nhập size của mảng:\n");
     int countUnsignedNumber;
     for(int i = 0; i < arryNumber; i++) {
         printf("Vui lòng nhập giá trị tại index %d\n", i);
         scanf("%d", &countUnsignedNumber);
     }
     for(int i = 0; i < arryNumber; i++) {
         printf("Giá trị phần tử tại index %d là : %d", i, )
     }